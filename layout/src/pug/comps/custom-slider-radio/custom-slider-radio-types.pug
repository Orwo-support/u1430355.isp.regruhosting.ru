.label-controll
    .label-controll__help.help-button(data-help-modal-id="helpModalTypeOfExecution")
        include ../../../img/icon-help.svg
        include ../../comps/help-modal/help-modal-type-of-execution

    .label-controll__caption Тип исполнения
    .label-controll__body
        .label-controll__content
            .radio-group#executionType(data-controller-type="radio-group")
                .marker
                //- Если нужно чтобы при инициализации был выбран какой-то пункт, добавить checked='' в соответсвующий input
                //- Список значений формировать динамически из параметров элементв на стороне сервера
                //- Тип свойства фильтра data-filter-property НЕ ТРОГАТЬ! именно так оно указано в JS
                input.input#typeOuter(type='radio' name='type' value='')
                label.filter-controller(for='typeOuter' data-filter-property="executionType" data-filter-value="outer") Уличный
                input.input#typeInner(type='radio' name='type' value='')
                label.filter-controller(for='typeInner' data-filter-property="executionType" data-filter-value="inner") Внутренний