.label-controll
    .label-controll__help.help-button(data-help-modal-id="helpModalTypicalSolutions")
        include ../../../img/icon-help.svg
        include ../../comps/help-modal/help-modal-typical-solutions

    .label-controll__caption Тип исполнения
    .label-controll__body
        .label-controll__content
            .radio-group#executionType(data-controller-type="radio-group")
                .marker
                // Если нужно чтобы при инициализации был выбран какой-то пункт, добавить checked='' в соответсвующий input
                // Список значений формировать динамически из параметров элементв на стороне сервера
                // Тип свойства фильтра data-filter-property НЕ ТРОГАТЬ! именно так оно указано в JS
                input#typeVertical(type='radio' name='type' value='')
                label.filter-controller(for='typeVertical' data-filter-property="executionType" data-filter-value="vertical") Вертикальный
                input#typeHorizontal(type='radio' name='type' value='')
                label.filter-controller(for='typeHorizontal' data-filter-property="executionType" data-filter-value="horizontal") Горизонтальный