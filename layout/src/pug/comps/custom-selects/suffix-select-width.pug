.label-controll
    .label-controll__caption Ширина
    .label-controll__body
        .label-controll__content

            // Добавлять класс custom-select всем кастомным селектам
            .select-suffix.custom-select#width(data-controller-type="custom-select")
                // Если есть значение по умолчанию добавить его в .select-suffix__selected-item.selected вместо &nbsp;
                .select-suffix__selected-item.selected &nbsp;
                .select-suffix__arr
                .select-suffix__delimiter
                .select-suffix__units мм
                .custom-select__container
                    .custom-select__list.scroller
                        // Если есть значение по умолчанию добавить .active соответствующему значению
                        // Список значений формировать динамически из параметров элементв на стороне сервера
                        // Тип свойства фильтра data-filter-property НЕ ТРОГАТЬ! именно так оно указано в JS
                        .custom-select__item.filter-controller(data-filter-property="width" data-filter-value="") Сбросить
                        .custom-select__item.filter-controller(data-filter-property="width" data-filter-value="3420") 3420
                        .custom-select__item.filter-controller(data-filter-property="width" data-filter-value="4000") 4000
                        .custom-select__item.filter-controller(data-filter-property="width" data-filter-value="5000") 5000
                        .custom-select__item.filter-controller(data-filter-property="width" data-filter-value="6000") 6000
                        .custom-select__item.filter-controller(data-filter-property="width" data-filter-value="7000") 7000